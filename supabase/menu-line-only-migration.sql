-- ============================================================================
--  施術メニューに「LINE限定メニュー」の印をつけられるようにする
--  agioの予約ページを、一般用（Googleマップなど）とLINE用の2つに分けて、
--  LINE限定価格のメニューが一般のお客様に見えないようにするために使う。
--  すでに schema.sql（または reset-and-rebuild.sql）を実行済みのプロジェクトで、
--  この内容だけ追加実行してください。
-- ============================================================================

alter table menu_items add column if not exists line_only boolean not null default false;
