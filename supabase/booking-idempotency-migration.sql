-- ============================================================================
--  お客様向け予約ページ（/book, /book/line）の二重送信・二重予約を防ぐための列
-- ============================================================================

alter table bookings add column if not exists idempotency_key text unique;
